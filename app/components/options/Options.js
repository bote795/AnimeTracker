import React from 'react'

class Options extends React.Component {
    render() { 
        return (
        <div>
            <button>edit</button>
        </div>
        );
    }
}

export default Options;